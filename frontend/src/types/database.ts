/**
 * Re-exports database types for backward compatibility.
 * Prefer importing from "@/types/database.types" for new code.
 */
export type {
  Database,
  Json,
  Profile,
  ProfileInsert,
  ProfileUpdate,
} from "@/types/database.types";
